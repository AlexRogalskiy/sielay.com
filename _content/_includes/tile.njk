{% extends 'card.njk' %}

{% block content %}
{% include 'ribbon.njk' %}
<div class="text-gray-900 font-bold text-xl mb-2 mt-2">
    <a href="{{ post.url }}">{{ post.data.title }}</a>
</div>
{% if post.data.image %}
<img class="w-full" src="{{ post.url }}{{ post.data.image }}"/>
{% endif %}
{% if post | text_length > 0 %}
<div class="text-gray-700 text-base limited hide-review">{% excerpt post %}… <a href="{{ post.url }}">read more…</a></div>
{% endif %}
{% endblock %}

{% block rest %}
<div class="mb-8 limited limited-low">
{%- for tag in post.data.tags -%}
<a class="inline-block bg-gray-200 rounded-full px-2 py-1 text-gray-600 mr-2 mb-1 text-xs" title="second" href="/blog/tag/{{ tag | blog_slug }}">#{{ tag }}</a>
{%- endfor -%}
</div>
<div class="flex items-center">
<div class="text-sm">
<p class="text-gray-900 leading-none"></p>
<p class="text-gray-600">{{ post | readingTime }} min read - {{ post.date | blog_dateformat('LL') }}</p>
</div>
</div>
{% endblock %}